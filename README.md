# AI_Maze_Solver
AI Maze Solver:An AI-powered maze solver that generates a maze as a CSV file and finds the shortest path using A (A-Star), DFS (Depth-First Search), and BFS (Breadth-First Search) algorithms.
